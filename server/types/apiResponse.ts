export interface apiResponse {
  msg: string;
  success: boolean;
  id?: string;
  role?: string;
  token?: string;
  // course?: Course;
  // allCourses?: [Course];
  // user?: User;
  // purchasedCourses?: [Course];
  // wishlist?: [Course];
}
